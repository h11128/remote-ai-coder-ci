---
description: "并发编程和系统设计模式、调试方法论 — 滑动窗口、环形缓冲区、共享状态、线程安全、时间窗口过期、缓存淘汰、subagent 并行、测试隔离、错误诊断。Concurrency, sliding window, ring buffer, thread safety, cache eviction, parallel subagent, test isolation, error diagnosis."
alwaysApply: false
---

# Coding Patterns

## 时间窗口/滑动窗口/缓存淘汰

实现了时间窗口/滑动窗口/缓存淘汰逻辑时，**必须测试"无新输入时旧条目是否正确过期"**。

**典型陷阱**: 只在有新数据到达时才检查过期 → 如果一段时间没有新数据，窗口内的旧条目永远不会被清理。

**测试检查清单**:
- 正常流: 数据持续到达，窗口正确滑动
- 无输入: 停止输入后等待超过窗口时长，验证旧数据被清除
- 边界: 恰好在窗口边界的数据，应该留还是去？

## 有状态缓冲区并发安全

有状态缓冲区（环形缓冲区、定时器队列、事件累加器）在多线程/多协程环境下**必须有同步机制**。

**检查清单**:
- 谁在写？谁在读？是否在不同线程？
- 是否需要原子操作、锁、或线程安全的数据结构？
- 清理/淘汰操作与读写操作是否有竞态条件？

## 并行 Subagent 文件冲突

多个 subagent 并行写入同一目录时，后执行的会覆盖先执行的。

**解决方案**:
- 确保 subagent 之间写入的文件路径完全不重叠
- subagent 完成后立即 read 验证每个输出文件的内容
- 如果有重叠风险，改为串行执行

## 测试失败隔离

测试失败时的三条规则：

1. **隔离优先**: 先用单个测试类运行（如 `--tests "com.xxx.SingleTestClass"`），拿到干净的错误信息再分析。绝不在全量运行的噪音里猜原因。
2. **OOM 是症状**: 遇到 `OutOfMemoryError` 先追问"什么在持续消耗资源？"（例如：测试超时 × N 个测试 = JVM 长时间高压），不要直接加 `maxHeapSize`。
3. **推翻失败假设**: 一个修复无效 → 必须推翻当前假设重新分析。两次同方向修复无效 = 方向错误。

## 错误诊断流程

工具/命令报错时的强制流程：

1. **读具体 error message** — 在回复中引用关键部分
2. **提出假设** — 基于 error message，不是基于"常见原因"
3. **验证假设** — 如怀疑 auth 问题 → 先跑 `auth-status` 验证
4. **给出结论**

**禁止**: 跳过错误内容直接说"session 过期"/"网络问题"/"缓存问题"等笼统归因。把"记忆里的常见原因"当作"不需要验证的事实"是错误的。
